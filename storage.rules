rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Node Writer: лише власник за uid у шляху
    match /node-writer/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
