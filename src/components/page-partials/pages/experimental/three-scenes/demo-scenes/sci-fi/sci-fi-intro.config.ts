/** Delay after first rendered frame before starting loading fade-out. */
export const sciFiSceneWarmupMs = 1600;

/** Loading overlay opacity transition (ms). */
export const sciFiLoadingFadeMs = 700;

/** Minimum frames in canvas before we consider the scene "warm". */
export const sciFiSceneReadyFrames = 4;
