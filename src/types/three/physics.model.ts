export enum RigidBodyType {
  fixed = "fixed",
  dynamic = "dynamic",
  kinematicPosition = "kinematicPosition",
  kinematicVelocity = "kinematicVelocity",
}

export enum RigidBodyColliders {
  false = "false",
  cuboid = "cuboid",
  ball = "ball",
  trimesh = "trimesh",
  hull = "hull",
}
